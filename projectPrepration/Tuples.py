'''
    --- Tuples is similar to list, so we can use them to store a list of items.
    --- Tuples are immutabel, we can't muteit
    --- for list using {}, for tuples using ().
    --- Once we declare tubple we can't change the value
'''

numbers = (3,4,5,7,8,35)
print(numbers[0])
numbers[0] = 9
